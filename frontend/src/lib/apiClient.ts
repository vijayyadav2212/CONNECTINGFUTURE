// Thin TypeScript wrapper to use the existing JS ApiClient under frontend/lib/auth
// This keeps imports consistent with '@/lib/...'
import apiClientJs from '../../lib/auth/apiClient';

export default apiClientJs;
